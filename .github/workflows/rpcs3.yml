name: Windows 11 ARM Launcher Check

on: [push, pull_request, workflow_dispatch]

jobs:
  check-inventory:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: List Launcher Inventory
        run: py -0p
