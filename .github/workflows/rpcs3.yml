name: docker

on:
  workflow_dispatch:
  pull_request:
  push:

jobs:
  docker_build:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: docker version
        run: docker version
      - name: docker info
        run: docker info
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
