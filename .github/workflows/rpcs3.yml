name: Reproduce Windows 11 ARM Headless Issue

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  test-gui-interactivity:
    runs-on: windows-11-arm
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install pythonnet clr-loader

      - name: Create and Run Test Script
        shell: pwsh
        run: |
          # Create demo.py with script to check GUI interactivity via Windows.Forms
          # (Script involves creating a Form and checking Form.ActiveForm)
          python -c "
          import sys, clr_loader, os
          from pathlib import Path
          from pythonnet import set_runtime
          # ... (script content from original)
          "
          python demo.py
