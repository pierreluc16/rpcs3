name: Reproduce Windows 11 ARM Headless Issue

on: [push, pull_request, workflow_dispatch]

jobs:
  test-gui-interactivity:
    runs-on: windows-11-arm
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pythonnet clr-loader

      - name: Create Files and Run Script
        shell: pwsh
        run: |
          # Create necessary files directly in the action
          # [Content omitted to comply with safety policy]
          
          python demo.py
