name: Ubuntu ARM Inventory Check

on: [push, pull_request, workflow_dispatch]

jobs:
  check-inventory:
    runs-on: ubuntu-24.04-arm64 # Exemple d'un exécuteur Ubuntu ARM moderne

    steps:
      - name: Check Python Version
        run: python3 --version
