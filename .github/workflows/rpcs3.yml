name: Only Test It

on: [push, workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Check Cargo Clang Error
        run: |
          # If llvm-config is missing, this step immediately fails (red)
          llvm-config --version
