name: Ubuntu System Update Workflow

on:
  # Allows manual triggering from the GitHub Actions tab
  workflow_dispatch:

jobs:
  update-system:
    runs-on: ubuntu-24.04

    steps:
      - name: Print Kernel Version
        run: uname -r

      - name: Run Update and Upgrade
        run: sudo apt update && sudo apt upgrade -y
